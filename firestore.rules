rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Allow all for development - TODO: Secure this in production!
    match /{document=**} {
      allow read, write: if true;
    }
    
    // // Helper functions
    // function isAuthenticated() {
    //   return request.auth != null;
    // }
    // 
    // function isAdmin() {
    //   return isAuthenticated() && request.auth.token.role == 'admin';
    // }
    // 
    // // Admins collection - only accessible by authenticated users
    // match /admins/{adminId} {
    //   allow read: if isAuthenticated();
    //   allow write: if isAuthenticated() && request.auth.uid == adminId;
    // }
    // 
    // // Students collection - only admins can manage
    // match /students/{studentId} {
    //   allow read: if isAuthenticated();
    //   allow create, update, delete: if isAuthenticated();
    // }
    // 
    // // Waste Types - only admins can manage
    // match /wasteTypes/{typeId} {
    //   allow read: if isAuthenticated();
    //   allow create, update, delete: if isAuthenticated();
    // }
    // 
    // // Waste Collections - read by all authenticated, write by admins
    // match /wasteCollections/{collectionId} {
    //   allow read: if isAuthenticated();
    //   allow create: if isAuthenticated();
    //   allow update, delete: if isAuthenticated() && request.auth.uid == resource.data.recordedBy;
    // }
    // 
    // // Settings - read by all, write by admins
    // match /settings/{settingId} {
    //   allow read: if isAuthenticated();
    //   allow write: if isAuthenticated();
    // }
  }
}